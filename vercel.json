{
    "version": 2,
    "builds": [
        {
            "src": "/client/",
            "use": "@vercel/static-build",
            "config": {"buildCommand": "npm run build"}
        },
        {
            "src": "/server/",
            "use": "@vercel/static-build",
            "config": {"buildCommand": "npm run build"}
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/server/dist/app.js"
        },
        {
            "src": "/(.*)",
            "dest": "/client/dist/$1"
        }

    ]
}